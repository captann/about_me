def mirror(arr):
    abc = arr.copy()  # создаем копию исходного списка arr
    abc.reverse()  # применяем метод reverse() к списку abc
    arr += abc  # теперь список arr - единый список abc и arr
# мы изменили список arr
#  += вызывает метод __iadd__,  который изменяет текущий объект

